type TProps = {
  title: string;
  subtitle: string;
  image: string;
  price: number;
  favorite: boolean;
  favoriteAction: () => void;
};

const ProductCard: React.FC<TProps> = () => {
  return null;
};

export default ProductCard;
